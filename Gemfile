source "https://rubygems.org"

# GitHub Pages Dependencies (Latest compatible versions)
gem "github-pages", "~> 232", group: :jekyll_plugins
gem "webrick", "~> 1.8"  # Required for Ruby 3.0+

# Additional plugins if not included in github-pages
group :jekyll_plugins do
  gem "jekyll-paginate"
  gem "jekyll-sitemap"
  gem "jekyll-feed"
end
